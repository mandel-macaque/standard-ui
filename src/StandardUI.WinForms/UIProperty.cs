namespace Microsoft.StandardUI.WinForms
{
    public class UIProperty : StandardUI.UIProperty
    {
        public UIProperty(string name, object? defaultValue, bool readOnly = false) : base(name, defaultValue, readOnly)
        {
        }
    }
}

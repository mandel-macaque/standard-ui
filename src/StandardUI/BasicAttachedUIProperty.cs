namespace Microsoft.StandardUI
{
    public class BasicAttachedUIProperty : BasicUIProperty
    {
        public BasicAttachedUIProperty(string name, object? defaultValue, bool readOnly = false)
            : base(name, defaultValue, readOnly)
        {
        }
    }
}

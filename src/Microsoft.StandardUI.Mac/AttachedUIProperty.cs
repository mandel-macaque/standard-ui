namespace Microsoft.StandardUI.Mac
{
    public class AttachedUIProperty : BasicAttachedUIProperty
    {
        public AttachedUIProperty(string name, object? defaultValue, bool readOnly = false) : base(name, defaultValue, readOnly)
        {
        }
    }
}
